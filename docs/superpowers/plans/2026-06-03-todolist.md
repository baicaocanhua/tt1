# TodoList Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a single-file TodoList web app that persists tasks in browser storage.

**Architecture:** `index.html` contains the complete app: markup, styles, and JavaScript state management. A small Node test validates the expected static structure and behavior hooks before manual browser verification.

**Tech Stack:** HTML, CSS, vanilla JavaScript, Node built-in test runner.

---

### Task 1: Add Static Test Coverage

**Files:**
- Create: `tests/todolist-static.test.mjs`

- [ ] **Step 1: Write the failing static test**

Create `tests/todolist-static.test.mjs` with assertions that `index.html` exposes the expected controls and JavaScript behavior hooks.

- [ ] **Step 2: Run the test to verify it fails**

Run: `node --test tests/todolist-static.test.mjs`

Expected: failure because `index.html` does not exist yet.

### Task 2: Implement the TodoList Page

**Files:**
- Create: `index.html`

- [ ] **Step 1: Create the page**

Create a single HTML file with the form, list, filter controls, persistence helpers, and rendering logic.

- [ ] **Step 2: Run the static test**

Run: `node --test tests/todolist-static.test.mjs`

Expected: pass.

### Task 3: Browser Verification

**Files:**
- Read: `index.html`

- [ ] **Step 1: Open the file in the in-app browser**

Open `file:///G:/tt1/index.html`.

- [ ] **Step 2: Verify core behavior**

Add a task, mark it complete, filter completed items, delete it, and confirm the empty state returns.
